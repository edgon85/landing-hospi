---
import '@fontsource-variable/open-sans';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Centro médico hospitalario profesional, con personal capacitado garantizando el bienestar de los pacientes"
    />
    <meta name="viewport" content="width=device-width" />

    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global>
  html {
    font-family: 'Open Sans Variable', system-ui, sans-serif;
    background: #f6f6f6;
  }
</style>
